# 功能全览

包含代码块、表格、任务列表的完整示例。

## 代码

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("FeatherView"))
```

```ts
interface RendererDefinition {
  id: string
  extensions: string[]
}
```

## 表格

| 编号 | 名称 | 说明 |
| --- | --- | --- |
| 1 | 首页 | 打开文件与最近记录 |
| 2 | 阅读页 | 渲染与搜索 |
| 3 | 设置页 | 主题与字号 |

## 任务

- [x] 初始化项目
- [x] 实现渲染器
- [ ] 发布安装包

## 引用

> 轻量、本地优先、启动快速。
